import type { SortOption, SpaceDefinition } from "./types";
import SearchBar from "./SearchBar";
import SpaceIcon from "./SpaceIcon";

type LibraryHeaderProps = { count: number; search: string; sort: SortOption; space: string; spaces: SpaceDefinition[]; onSearchChange: (value: string) => void; onSortChange: (sort: SortOption) => void; onSpaceChange: (space: string) => void; onCreateSpace: () => void };

export default function LibraryHeader({ count, search, sort, space, spaces, onSearchChange, onSortChange, onSpaceChange, onCreateSpace }: LibraryHeaderProps) {
  return <header className="mb-8 flex flex-col gap-5 sm:mb-10"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#a36347]">Your workspace</p><div className="mt-2 flex flex-wrap items-baseline gap-x-3 gap-y-1"><h1 className="font-['Instrument_Serif',Georgia,serif] text-4xl tracking-[-0.035em] text-[#3d3028] sm:text-[2.8rem]">Library</h1><span className="text-sm text-[#8b7b70]">{count} {count === 1 ? "document" : "documents"}</span></div></div><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex flex-wrap gap-2"><button className={`rounded-md px-2.5 py-1.5 text-xs font-semibold ${space === "all" ? "bg-[#f1e4d9] text-[#824f38]" : "text-[#837369] hover:bg-[#f8eee6]"}`} onClick={() => onSpaceChange("all")}>All spaces</button>{spaces.map((item) => <button key={item.id} className={`inline-flex items-center gap-1.5 rounded-md px-2.5 py-1.5 text-xs font-semibold ${space === item.id ? "bg-[#f1e4d9] text-[#824f38]" : "text-[#837369] hover:bg-[#f8eee6]"}`} onClick={() => onSpaceChange(item.id)}><SpaceIcon icon={item.icon} size={13} />{item.label}</button>)}<button className="inline-flex items-center gap-1 rounded-md px-2.5 py-1.5 text-xs font-semibold text-[#a36347] hover:bg-[#f8eee6]" onClick={onCreateSpace}>+ New space</button></div><div className="flex flex-col gap-3 sm:flex-row sm:items-center"><SearchBar value={search} onChange={onSearchChange} /><label className="flex items-center gap-2 text-xs font-medium text-[#7d6d62]">Sort by<select value={sort} onChange={(event) => onSortChange(event.target.value as SortOption)} className="cursor-pointer border-0 bg-transparent py-2 text-xs font-semibold text-[#58483e] outline-none"><option value="recent">Recently Edited</option><option value="oldest">Oldest</option><option value="alphabetical">Alphabetical</option><option value="words">Word Count</option></select></label></div></div></header>;
}
